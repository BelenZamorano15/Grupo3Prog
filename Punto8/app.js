// Realizado por: Luz Luna

let num = parseInt(prompt("Ingrese un numero: "));
if (num % 2 == 0) {
    console.log ("El numero es divisible en 2");
} else {
    console.log ("El numero no es divisible en 2");
}
